public class MyTest
{
    /**
     * Constructor for objects of class MyTest
     * @param name This is the number
     * @param city This is the city
     */
    public MyTest(String name, String city)
    {
  
    } // end of MyTest
    
    /**

    /**
     * Raise the bar.
     */
    public String up(String x)
    {
        String result;
        if (x.equals("")) {
            result = "a";
        }
        else {
            result = "b";
        }
        HashSet<Integer> set = new Has<X>();
        char ch = result.charAt(0);
        int i = ch;
        for (Integer eachNum : set)
        {
            if (eachNum < 100) {}
        }
        return result;
    }

}
